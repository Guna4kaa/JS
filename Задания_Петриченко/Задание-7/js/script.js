const button = document.getElementById('start');
console.log(button);

const blocks = document.querySelectorAll('[class$=-value]');
console.log(blocks);

const mandatoryExp = document.querySelectorAll('.expenses-item');
console.log(mandatoryExp);

const aprButton = document.getElementsByTagName('button');
console.log(aprButton);

const noMandatory = document.querySelectorAll('.optionalexpenses-item');
console.log(noMandatory);

